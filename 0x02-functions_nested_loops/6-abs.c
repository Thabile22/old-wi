#include "main.h"

/**
 * _abs - Entry point
 * Description - Print the absolute value of an integer
 * @n: Integer of abs value
 * Return: int
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
